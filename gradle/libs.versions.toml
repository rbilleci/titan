# Central version catalog (audit G-12, plan 4.5).
#
# Version decisions:
# - junit: 5.10.2 — already the de-facto version in every module; centralised, not bumped
#   (version bumps are deliberate, separate changes).
# - mysql-connector-j: 8.4.0 everywhere. The repo had 8.4.0 (titan-runtime-jdbc,
#   titan-gradle-plugin) and 9.0.0 (titan-transpiler testRuntimeOnly) side by side. 8.4.x is the
#   LTS line and matches the mysql:8.4 Testcontainers image the suites run against; 9.0.0 is an
#   innovation release with a short support window. Aligned down to the LTS.
# - testcontainers: 1.21.4 — already uniform; centralised.
# - errorprone: build-time-only static analysis (never a runtime dependency of any module,
#   per the no-framework-dependencies rule).

[versions]
junit = "5.10.2"
junit4 = "4.13.2"
mockito = "5.12.0"
archunit = "1.3.0"
testcontainers = "1.21.4"
postgresql = "42.7.4"
mysql = "8.4.0"
jmh = "1.37"
errorprone = "2.41.0"
errorprone-plugin = "4.3.0"
intellij-plugin = "1.17.4"

[libraries]
junit-bom = { module = "org.junit:junit-bom", version.ref = "junit" }
junit-jupiter = { module = "org.junit.jupiter:junit-jupiter" }
junit-jupiter-api = { module = "org.junit.jupiter:junit-jupiter-api", version.ref = "junit" }
junit4 = { module = "junit:junit", version.ref = "junit4" }
mockito-core = { module = "org.mockito:mockito-core", version.ref = "mockito" }
archunit-junit5 = { module = "com.tngtech.archunit:archunit-junit5", version.ref = "archunit" }
testcontainers-core = { module = "org.testcontainers:testcontainers", version.ref = "testcontainers" }
testcontainers-junit-jupiter = { module = "org.testcontainers:junit-jupiter", version.ref = "testcontainers" }
testcontainers-postgresql = { module = "org.testcontainers:postgresql", version.ref = "testcontainers" }
testcontainers-mysql = { module = "org.testcontainers:mysql", version.ref = "testcontainers" }
postgresql = { module = "org.postgresql:postgresql", version.ref = "postgresql" }
mysql-connector-j = { module = "com.mysql:mysql-connector-j", version.ref = "mysql" }
jmh-core = { module = "org.openjdk.jmh:jmh-core", version.ref = "jmh" }
jmh-generator-annprocess = { module = "org.openjdk.jmh:jmh-generator-annprocess", version.ref = "jmh" }
errorprone-core = { module = "com.google.errorprone:error_prone_core", version.ref = "errorprone" }

[plugins]
errorprone = { id = "net.ltgt.errorprone", version.ref = "errorprone-plugin" }
intellij = { id = "org.jetbrains.intellij", version.ref = "intellij-plugin" }
